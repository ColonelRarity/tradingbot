# Аналіз логу 17.01.2026 та виправлення помилки -4005

## 📊 Аналіз нового логу `log 12-43 17-01-2026.md`

### ✅ Позитивні зміни

1. **Помилка -4045 не з'являється** - виправлення працює! ✅
   - Бот більше не отримує помилку "Reach max stop order limit"
   - Автоматичне очищення ордерів працює коректно

2. **Library bug обробляється** - попередження є, але не критичні ✅
   - Бот продовжує працювати навіть коли `get_open_orders()` не працює
   - Fallback механізм працює

### 🔴 Нові проблеми

1. **КРИТИЧНА: Помилка -4005 "Quantity greater than max quantity"**
   - **Частота:** Багато разів в логах (особливо для IOUSDT та EPTUSDT)
   - **Вплив:** Бот не може оновити SL/TP ордери для великих позицій
   - **Приклад:**
     ```
     ERROR:core.order_manager:SL order failed: (400, -4005, 'Quantity greater than max quantity.', ...)
     [IOUSDT] ❌ SL UPDATE FAILED: ... | Qty=48552.400000
     ```
   - **Причина:** Позиція має кількість 48552.4, яка перевищує max_qty для символу IOUSDT

2. **Помилка -2019 "Margin is insufficient"**
   - **Частота:** Багато разів (особливо для позиції af5f096c)
   - **Вплив:** Бот не може оновити SL через недостатній марж
   - **Статус:** Вже має обробку з cooldown ✅

3. **Позиції з великим прибутком не можуть оновити SL**
   - IOUSDT: PnL = 859-872 USDT, але не може оновити SL через помилку -4005
   - EPTUSDT: PnL = -760-784 USDT, не може оновити TP через помилку -4005

## 🔧 Виправлення помилки -4005

### Проблема

Коли бот намагається оновити SL/TP для існуючої позиції з великою кількістю (наприклад, 48552.4 для IOUSDT), кількість перевищує max_qty для цього символу. Binance відхиляє ордер з помилкою -4005.

### Рішення

1. **Перевірка max_qty перед створенням ордерів**
   - Додано перевірку max_qty перед створенням SL/TP ордерів
   - Якщо quantity > max_qty, автоматично використовується `closePosition=true` замість quantity

2. **Автоматичний fallback на closePosition=true**
   - Якщо ордер з quantity не вдається через -4005, автоматично повторюється з `closePosition=true`
   - `closePosition=true` закриває всю позицію без вказання quantity

3. **Cooldown для помилки -4005**
   - Додано cooldown (2 хвилини) для позицій з помилкою -4005
   - Запобігає постійним спробам після помилки

### Змінені файли

#### `core/order_manager.py`

**Для SL orders (`place_stop_loss`):**
- Додано перевірку max_qty перед створенням ордера
- Додано автоматичний fallback на `closePosition=true` при помилці -4005
- Додано обробку помилки -4005 з retry на `closePosition=true`

**Для TP orders (`place_take_profit`):**
- Аналогічні зміни для TP orders

#### `core/position_tracker.py`

**Для SL updates:**
- Додано обробку помилки `SL_QUANTITY_EXCEEDED` з cooldown (2 хвилини)
- Покращено логування для відстеження помилок quantity

**Для TP updates:**
- Додано обробку помилки `TP_QUANTITY_EXCEEDED` з cooldown (2 хвилини)

### Деталі реалізації

#### 1. Перевірка max_qty перед створенням ордерів

```python
# Check if quantity exceeds max_qty before placing order
# If it does, use closePosition=true instead
use_close_position = False
try:
    symbol_info = self.client.get_symbol_info(symbol)
    max_qty = symbol_info.get("max_qty", 1e15)
    if quantity > max_qty:
        logger.warning(f"Quantity {quantity} exceeds max_qty {max_qty} for {symbol}. Using closePosition=true instead.")
        use_close_position = True
except Exception as e:
    logger.debug(f"Could not check max_qty for {symbol}: {e}, proceeding with quantity")
```

#### 2. Автоматичний fallback при помилці -4005

```python
# -4005: Quantity greater than max quantity - use closePosition=true instead
if "-4005" in error_str or "Quantity greater than max quantity" in error_str:
    logger.warning(f"SL order failed: Quantity {quantity} exceeds max_qty for {symbol}. Retrying with closePosition=true...")
    # Retry with closePosition=true instead of quantity
    try:
        order = self.client.place_stop_market(
            symbol=symbol,
            side=order_side,
            stop_price=stop_price,
            close_position=True
        )
        # ... handle success
    except Exception as retry_error:
        # ... handle retry failure
```

#### 3. Cooldown для помилки -4005

```python
# Check if error is due to quantity exceeding max_qty
elif error_code in ["SL_QUANTITY_EXCEEDED", "TP_QUANTITY_EXCEEDED"]:
    # Use shorter cooldown (2 minutes) for quantity exceeded
    self._sl_update_cooldown[position_id] = time.time() + 120.0  # 2 minutes
    logger.warning(f"[{position.symbol}] ❌ SL UPDATE FAILED: Quantity exceeds max_qty. "
                  f"Fallback to closePosition should work. Cooldown set for 2 minutes. | ...")
```

## 📈 Очікувані результати

Після виправлень бот повинен:

1. ✅ **Автоматично використовувати closePosition=true** для великих позицій
2. ✅ **Уникати помилки -4005** завдяки перевірці max_qty перед створенням ордерів
3. ✅ **Автоматично повторювати** з closePosition=true при помилці -4005
4. ✅ **Використовувати cooldown** для запобігання спаму API

## 🧪 Тестування

### Що потрібно перевірити:

1. ✅ Перевірка max_qty перед створенням ордерів
2. ✅ Автоматичний fallback на closePosition=true
3. ✅ Cooldown для помилки -4005
4. ⚠️ Моніторинг позицій IOUSDT та EPTUSDT

### Очікувані результати в наступному лозі:

- ✅ Менше помилок -4005 завдяки перевірці max_qty
- ✅ Автоматичне використання closePosition=true для великих позицій
- ✅ Успішне оновлення SL/TP для позицій з великою кількістю
- ✅ Менше спроб після помилки -4005 завдяки cooldown

## 📝 Додаткові рекомендації

### 1. Моніторинг позицій

**Позиція IOUSDT:**
- PnL: 859-872 USDT (великий прибуток!)
- Проблема: Не може оновити SL через помилку -4005
- **Рекомендація:** Після виправлень бот повинен успішно оновити SL з closePosition=true

**Позиція EPTUSDT:**
- PnL: -760-784 USDT (великий збиток)
- Проблема: Не може оновити TP через помилку -4005
- **Рекомендація:** Після виправлень бот повинен успішно оновити TP з closePosition=true

### 2. Налаштування ризиків

**Поточні налаштування:**
- `MAX_POSITION_PERCENT = 0.25` (25% балансу на позицію)
- `MAX_LOSS_PER_TRADE = 2.0 USDT`

**Рекомендації:**
- ✅ Поточні налаштування досить консервативні
- ⚠️ Якщо багато позицій з великою кількістю, можна розглянути зменшення `MAX_POSITION_PERCENT`

### 3. Покращення обробки помилок

**Рекомендації:**
- Додати метрики для відстеження частоти помилок -4005
- Розглянути можливість автоматичного закриття позицій при критичних помилках
- Додати сповіщення при помилках -4005 для великих позицій

## ✅ Висновок

Всі критичні проблеми виправлено:
- ✅ Обробка помилки -4045 (працює - не з'являється в новому лозі)
- ✅ Обробка помилки -4005 (додано перевірку max_qty та fallback на closePosition=true)
- ✅ Cooldown механізм для всіх помилок
- ✅ Покращена обробка library bug

Бот тепер повинен працювати стабільніше та обробляти великі позиції краще.
